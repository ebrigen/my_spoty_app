name: my_spoty_app
description: A Flutter music app
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"   # Dart 3-compatible (you’re on 3.9.2)

dependencies:
  flutter:
    sdk: flutter

  # Your packages (one key only, no duplicates)
  audioplayers: ^5.2.1
  http: ^1.2.2
  path_provider: ^2.1.3
  #ffmpeg_kit_flutter_min_gpl: ^6.0.3
  ffmpeg_kit_min_gpl: ^6.0.3+2-LTS
  youtube_explode_dart: ^2.3.6
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/