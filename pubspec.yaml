name: my_spoty_app
description: A Flutter music app
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.5.1
  http: ^1.2.2
  path_provider: ^2.1.3
  ffmpeg_kit_min_gpl: ^6.0.3+2-LTS
  youtube_explode_dart: ^3.0.5
  cupertino_icons: ^1.0.8

dependency_overrides:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
